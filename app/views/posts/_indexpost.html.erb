<div id="<%= dom_id post %>">

  <%= link_to post_path(post), class: "card card-link" do %>
    <div class="card-body">
      <h5 class="card-title"><%= post.title %></h5>
      <h6 class="card-subtitle mb-2 text-muted"><%= post.user.email %></h6>
    </div>
  <% end %>

</div>
